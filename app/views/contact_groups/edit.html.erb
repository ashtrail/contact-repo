<h1>Editing Contact Group</h1>

<%= render 'form', contact_group: @contact_group %>

<%= button_to 'Destroy', contact_group_path(@contact_group), method: :delete, data: { confirm: 'Are you sure?' } %>
<br />
<br />
<%= link_to 'Show', @contact_group %> |
<%= link_to 'Back', contact_groups_path %>
