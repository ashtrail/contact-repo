<div class="columns">
  <div class="column is-half">
    <%= bulma_form_with model: common_interest do |form| %>
      <% if common_interest.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(common_interest.errors.count, "error") %> prohibited this common_interest from being saved:</h2>

          <ul>
            <% common_interest.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.text_field :name %>

      <div class="actions">
        <%= form.submit nil, class: "button is-primary mt-3" %>
      </div>
    <% end %>
  </div>
</div>
