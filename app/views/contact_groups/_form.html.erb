<div class="columns">
  <div class="column is-half">
    <%= bulma_form_with(model: contact_group) do |form| %>
      <% if contact_group.errors.any? %>
        <div id="error_explanation">
          <h2><%= pluralize(contact_group.errors.count, "error") %> prohibited this contact_group from being saved:</h2>

          <ul>
            <% contact_group.errors.each do |error| %>
              <li><%= error.full_message %></li>
            <% end %>
          </ul>
        </div>
      <% end %>

      <%= form.text_field :name %>

      <div class="actions">
        <%= form.submit nil, class: "button is-primary mt-3" %>
      </div>
    <% end %>
  </div>
</div>

